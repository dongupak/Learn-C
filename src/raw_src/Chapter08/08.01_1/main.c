#include <stdio.h>

int main() {
    int a = 10;        // 정수형 변수 선언 및 초기화

    printf("변수 a의 값: %d\n", a);          // 변수의 실제 값 출력
    printf("변수 a의 메모리 주소: %p\n", &a);  // 변수 a의 메모리 주소를 16진수로 출력
}